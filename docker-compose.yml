services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_POCKETBASE_URL=https://pb.calculatucasa.com
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: always
